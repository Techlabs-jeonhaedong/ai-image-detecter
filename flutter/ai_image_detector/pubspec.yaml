name: ai_image_detector
description: "Flutter on-device AI image detection using ONNX Runtime. Detects AI-generated images offline."
version: 0.1.0
homepage:

environment:
  sdk: ^3.10.4
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_onnxruntime: ^1.3.0
  image: ^4.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  # assets registered here are for the package itself (tests only).
  # App-level assets (model files) must be registered in the host app's pubspec.yaml.
